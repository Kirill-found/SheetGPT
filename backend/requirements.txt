# FastAPI и веб-сервер
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# База данных
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# AI модели
openai==1.10.0              # GPT-4o for production

# Аутентификация
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Google OAuth & API
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.147.0  # For Google Sheets API (formula executor)

# Утилиты
python-dotenv==1.0.0
httpx==0.28.1

# Данные и вычисления
pandas==2.2.0
numpy==1.26.3

# Web Search (100% free, no API keys)
ddgs==9.9.1

# FORCE REBUILD: 18 Nov 2025 - v7.4.0-WEB-SEARCH
# NEW: DuckDuckGo web search integration for real-time data
# AI can now generate tables with fresh data from the internet
# CRITICAL: web_search feature for generating tables from real-time internet data
# Railway MUST rebuild when requirements.txt changes
