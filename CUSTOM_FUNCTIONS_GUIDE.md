# SheetGPT Custom Functions v2.0.0 - Руководство пользователя

## 🚀 Что нового в v2.0.0?

**Одна функция вместо четырех!**

Теперь не нужно запоминать `GPT_VALUE`, `GPT_LIST`, `GPT_TABLE` - просто используй **=GPT()** для всего!

AI автоматически определяет формат ответа:
- 📝 Текст → Возвращает текст
- 🔢 Число → Возвращает число
- 📊 Список → Возвращает вертикальный список
- 📈 Таблица → Возвращает таблицу с заголовками

---

## 📦 Установка

1. Откройте Google Sheets
2. **Расширения** → **Apps Script**
3. Создайте новый файл `CustomFunctions.gs`
4. Скопируйте код из `CustomFunctions.gs`
5. Нажмите **Сохранить** (Ctrl+S)
6. Вернитесь в Google Sheets и используйте функции!

---

## 📚 Единственная функция - GPT()

### Синтаксис

```
=GPT(query, [dataRange])
```

**Параметры:**
- `query` (обязательный) - ваш вопрос или запрос к AI
- `dataRange` (опциональный) - диапазон данных для анализа (например, A1:C10)

**Возвращает:** Текст, число, список или таблицу (в зависимости от запроса)

---

## 💡 Примеры использования

### Пример 1: Текстовый ответ 📝

```
=GPT("Кто лучший менеджер по продажам?", A1:C10)
```

**Результат:**
```
Лучший менеджер: Иванов с общей суммой продаж 152,000₽
```

---

### Пример 2: Числовое значение 🔢

```
=GPT("Какая сумма продаж?", A1:C10)
```

**Результат:**
```
262000
```

AI автоматически понимает что нужно число и извлекает его из ответа!

**Ключевые слова для чисел:**
- сумма, среднее, количество
- итого, всего, макс, мин
- процент, сколько

---

### Пример 3: Вертикальный список 📊

```
=GPT("Топ 5 продуктов по продажам", A1:C10)
```

**Результат:**
```
| Ноутбук    |
| Телефон    |
| Монитор    |
| Клавиатура |
| Мышка      |
```

AI видит что нужен список и возвращает вертикальный массив.

---

### Пример 4: Таблица 📈

```
=GPT("Группировка по менеджерам с суммой продаж", A1:C10)
```

**Результат:**
```
| Менеджер | Сумма   |
|----------|---------|
| Иванов   | 152000  |
| Петров   | 80000   |
| Сидоров  | 30000   |
```

AI создает таблицу с заголовками и данными.

---

## 🎯 Практические сценарии

### Сценарий 1: Анализ продаж

**Данные:**
```
| Менеджер | Продукт  | Сумма   | Дата       |
|----------|----------|---------|------------|
| Иванов   | Ноутбук  | 150000  | 2024-01-15 |
| Петров   | Телефон  | 80000   | 2024-01-16 |
| Иванов   | Мышка    | 2000    | 2024-01-17 |
| Сидоров  | Монитор  | 25000   | 2024-01-18 |
```

**Запросы:**

```
=GPT("Кто лучший менеджер?", A1:D5)
→ "Лучший менеджер: Иванов с суммой 152,000₽"

=GPT("Сумма продаж Иванова", A1:D5)
→ 152000

=GPT("Топ 3 продукта", A1:D5)
→ | Ноутбук  |
  | Монитор  |
  | Телефон  |

=GPT("Группировка по менеджерам", A1:D5)
→ | Менеджер | Сумма  |
  | Иванов   | 152000 |
  | Петров   | 80000  |
  | Сидоров  | 25000  |
```

---

### Сценарий 2: Работа с датами

**Данные:**
```
| Дата       | Продажи |
|------------|---------|
| 2024-01-15 | 150000  |
| 2024-02-10 | 200000  |
| 2024-01-20 | 180000  |
| 2024-02-15 | 220000  |
```

**Запросы:**

```
=GPT("Сколько продаж за январь?", A1:B5)
→ 330000

=GPT("Средние продажи по месяцам", A1:B5)
→ | Месяц  | Среднее |
  | Январь | 165000  |
  | Февраль| 210000  |

=GPT("Топ 3 дня по продажам", A1:B5)
→ | 2024-02-15 |
  | 2024-02-10 |
  | 2024-01-20 |
```

---

### Сценарий 3: Текстовый анализ

**Данные:**
```
| Клиент  | Комментарий            | Статус  |
|---------|------------------------|---------|
| Иванов  | Срочный заказ!        | Ожидает |
| Петров  | Все ок                | Готово  |
| Сидоров | Нужно срочно доставить| Ожидает |
```

**Запросы:**

```
=GPT("Найди все срочные заказы", A1:C4)
→ | Иванов  |
  | Сидоров |

=GPT("Сколько заказов в статусе Ожидает?", A1:C4)
→ 2

=GPT("Список клиентов с готовыми заказами", A1:C4)
→ "Петров"
```

---

## 🔧 Дополнительная функция: GPT_DEBUG()

Для отладки используй `=GPT_DEBUG()` - она показывает полный JSON ответ от API:

```
=GPT_DEBUG("Кто лучший менеджер?", A1:C10)
```

**Результат:**
```json
{
  "response_type": "function_call",
  "function_used": "group_by_column",
  "summary": "Лучший менеджер: Иванов (152,000₽)",
  "structured_data": {
    "headers": ["Менеджер", "Сумма"],
    "rows": [["Иванов", 152000], ["Петров", 80000]]
  },
  "confidence": 0.98
}
```

Это помогает понять:
- Какая функция была использована (из 100 доступных)
- Какие данные вернул backend
- Почему результат выглядит именно так

---

## ⚙️ Настройки

### Изменить backend URL

По умолчанию используется Railway production:
```javascript
const API_URL_FUNCTIONS = 'https://sheetgpt-production.up.railway.app';
```

Для локального тестирования измените на:
```javascript
const API_URL_FUNCTIONS = 'http://localhost:8000';
```

---

## 🔥 Pro Tips

### 1. Будьте конкретны

✅ **Хорошо:**
```
=GPT("Сумма продаж Иванова в январе", A1:D100)
```

❌ **Плохо:**
```
=GPT("Покажи данные", A1:D100)
```

### 2. Указывайте диапазон данных

✅ **Быстро:**
```
=GPT("Анализ", A1:C100)  // Только 100 строк
```

❌ **Медленно:**
```
=GPT("Анализ")  // Весь лист (может быть 10,000+ строк)
```

### 3. Кэшируйте результаты

Формулы пересчитываются каждый раз при открытии листа. Чтобы избежать повторных API запросов:

1. Скопируйте ячейку с формулой (Ctrl+C)
2. Правой кнопкой → **Специальная вставка** → **Только значения**

### 4. Комбинируйте с другими формулами

```
=IF(GPT("Средняя зарплата", A1:B100) > 100000, "Высокая", "Низкая")

=GPT("Сумма продаж", A1:C10) * 1.2  // +20% наценка

=VLOOKUP(GPT("Лучший менеджер", A1:C10), E1:F10, 2, FALSE)
```

### 5. Используйте ключевые слова

Для **чисел** используйте:
- "сумма", "среднее", "количество"
- "итого", "всего", "макс", "мин"

Для **списков** используйте:
- "топ N", "список", "перечисли"

Для **таблиц** используйте:
- "группировка", "сводная таблица", "статистика"

---

## ⚠️ Ограничения

### Скорость
- AI запросы занимают **3-10 секунд**
- Таблицы быстрее (~2-3s)
- Сложный анализ медленнее (~8-10s)

### Лимиты
- Railway бесплатный план имеет лимиты
- OpenAI API: 30,000 токенов в минуту

### Размер данных
- Рекомендуется до **500 строк**
- Больше строк = медленнее + дороже

### Обновление
- Формулы пересчитываются при изменении данных
- Используйте кэширование для статических результатов

---

## 🐛 Troubleshooting

### Ошибка: "Ошибка: укажите запрос"
→ Первый параметр не может быть пустым

### Ошибка: "Exception: Request failed..."
→ Проверьте интернет соединение или backend API
→ Проверьте: https://sheetgpt-production.up.railway.app/health

### Формула возвращает странный результат
→ Попробуйте переформулировать запрос более конкретно
→ Используйте `=GPT_DEBUG()` чтобы увидеть полный ответ

### Долго считается (>30 секунд)
→ Уменьшите диапазон данных
→ Используйте более простой запрос
→ Проверьте лимиты Railway

### Возвращает текст вместо числа
→ Добавьте ключевые слова: "сумма", "среднее", "количество"
→ Пример: ~~"Продажи Иванова"~~ → "**Сумма** продаж Иванова"

### Возвращает текст вместо таблицы
→ Используйте слова: "группировка", "таблица", "сводка"
→ Пример: ~~"По менеджерам"~~ → "**Группировка** по менеджерам"

---

## 📊 Что умеет SheetGPT? (100 функций)

AI автоматически выбирает одну из 100 функций в зависимости от вашего запроса:

### Математика (8)
Сумма, среднее, медиана, процентили, ст.отклонение, дисперсия, корреляция, взвешенное среднее

### Фильтрация (20)
По условию, по дате, топ N, последние N, уникальные, дубликаты, содержит текст, пустые, между значениями

### Группировка (22)
Группировка по колонкам, сводные таблицы, агрегация (сумма/среднее/количество), накопительные итоги

### Сортировка (15)
Сортировка, ранжирование, процентный ранг, dense rank, row number, разбивка на группы

### Текстовые операции (10)
Поиск, regex, конкатенация, разделение, извлечение чисел/дат, замена, форматирование

### Работа с датами (10)
Форматирование, извлечение год/месяц/день, разница дат, добавление/вычитание дней, фильтры

### Действия (10)
Подсветка строк, создание таблиц/графиков, добавление/удаление колонок, обновление ячеек

### Инсайты (5)
Анализ трендов, поиск аномалий, рекомендации, генерация резюме, сравнение периодов

---

## 📖 Дополнительные ресурсы

- **API Docs:** https://sheetgpt-production.up.railway.app/docs
- **README:** [README.md](README.md)
- **Deployment Guide:** [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
- **Chrome Extension:** Доступно отдельно

---

## 🎉 Примеры для копирования

### Продажи
```
=GPT("Сумма продаж", A1:C100)
=GPT("Средняя продажа", A1:C100)
=GPT("Топ 5 менеджеров", A1:C100)
=GPT("Группировка по менеджерам с суммой", A1:C100)
```

### Финансы
```
=GPT("Общая выручка", A1:E100)
=GPT("Процент роста к прошлому месяцу", A1:E100)
=GPT("Расходы больше 50000", A1:E100)
```

### HR
```
=GPT("Средняя зарплата", A1:D100)
=GPT("Количество сотрудников в Москве", A1:D100)
=GPT("Топ 3 отдела по численности", A1:D100)
```

### Маркетинг
```
=GPT("Какой канал приносит больше лидов?", A1:F100)
=GPT("Конверсия по источникам", A1:F100)
=GPT("Стоимость лида по каналам", A1:F100)
```

---

**Версия:** 2.0.0
**Дата:** 2024-11-19
**Автор:** SheetGPT Team
**Лицензия:** MIT

**Попробуйте прямо сейчас:** `=GPT("Привет от SheetGPT!")` 🚀
