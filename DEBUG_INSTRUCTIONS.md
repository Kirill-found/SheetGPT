# 🔍 Диагностика проблемы с диаграммами

## Шаг 1: Откройте консоль браузера

1. **В Google Sheets** (где открыт SheetGPT)
2. Нажмите **F12** (или **Ctrl+Shift+I** на Windows)
3. Перейдите на вкладку **Console** (Консоль)

## Шаг 2: Воспроизведите ошибку

1. Напишите запрос: **"построй диаграмму по этим товарам"**
2. Отправьте
3. **Смотрите в консоль** — там появятся красные ошибки

## Что искать в консоли:

### ❌ Если видите ошибку типа:
```
[UI] Creating table and chart from structured_data
[UI] Table creation failed: createTableAndChart is not defined
```
**Проблема:** Code.gs не обновлен в Apps Script

### ❌ Если видите:
```
ReferenceError: google is not defined
```
**Проблема:** Sidebar.html работает вне Google Sheets

### ❌ Если видите:
```
Exception: Cannot call SpreadsheetApp.getActiveSpreadsheet() from this context
```
**Проблема:** Apps Script скрипт не привязан к таблице

### ✅ Если консоль показывает:
```
[UI] Creating table and chart from structured_data
[UI] Table created successfully
```
**Диаграмма должна работать!**

## Шаг 3: Проверьте Apps Script

1. **Расширения → Apps Script**
2. Найдите в Code.gs строку примерно **997**:
   ```javascript
   function createTableAndChart(structuredData) {
   ```
3. **Если этой функции НЕТ** → Code.gs НЕ обновлен!

## Шаг 4: Если функции нет — обновите вручную

### Для Code.gs:
1. Откройте: https://raw.githubusercontent.com/Kirill-found/SheetGPT/main/Code.gs
2. Ctrl+A → Ctrl+C (скопировать всё)
3. В Apps Script: выберите Code.gs → Ctrl+A → Delete → Ctrl+V
4. Ctrl+S (сохранить)

### Для Sidebar.html:
1. Откройте: https://raw.githubusercontent.com/Kirill-found/SheetGPT/main/Sidebar.html
2. Ctrl+A → Ctrl+C
3. В Apps Script: выберите Sidebar.html → Ctrl+A → Delete → Ctrl+V
4. Ctrl+S

## Шаг 5: Перезагрузка

1. **Закройте Apps Script** редактор
2. **Вернитесь в Google Sheets**
3. **Перезагрузите страницу**: F5 или Ctrl+R
4. **Откройте SheetGPT** снова
5. Попробуйте: "построй диаграмму по товарам"

---

## Частые ошибки:

### 🔴 "Я скопировал в C:\SheetGPT\frontend\Code.gs"
❌ **НЕПРАВИЛЬНО!** Это локальный файл на вашем компьютере.

✅ **ПРАВИЛЬНО:** Нужно копировать в **облачный редактор Apps Script** в Google Sheets!

### 🔴 "Я обновил файлы, но не перезагружал Google Sheets"
Apps Script кэширует старые версии. **Обязательно** перезагрузите страницу (F5).

### 🔴 "Я не вижу функцию createTableAndChart в Code.gs"
Значит вы **НЕ обновили** файл в облачном редакторе. Повторите Шаг 4.

---

## Быстрая проверка версии:

В Code.gs найдите строку примерно **427**:
```javascript
structured_data: result.structured_data || null,
```

**Если этой строки НЕТ** → Code.gs старой версии!

---

## Если ничего не помогает:

1. Откройте F12 консоль
2. Скопируйте **ВСЕ ошибки** (красный текст)
3. Отправьте мне полный текст ошибок
4. Я пойму, что конкретно не работает
