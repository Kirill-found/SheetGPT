# Админ-панель Telegram Bot - Руководство

## Обзор

Админ-панель предоставляет полный контроль над пользователями и статистикой SheetGPT Telegram Bot.

## Команды администратора

### 📊 /dashboard - Главная панель

Показывает полную статистику:
- **Пользователи**: общее количество, Premium/Free, конверсия
- **Рост**: новые пользователи за 24ч и 7 дней
- **Активность**: активные пользователи за 24ч и 7 дней
- **Запросы**: всего, сегодня, среднее на пользователя
- **Топ-5 активных пользователей**
- **Последние регистрации**

**Кнопки:**
- 🔄 Обновить - обновить статистику
- 👥 Пользователи - список всех пользователей
- 💎 Premium - список платящих пользователей
- 🆓 Free - список бесплатных пользователей
- 📊 Экспорт данных - скачать CSV файл

**Пример:**
```
/dashboard
```

---

### 👥 /users [page] - Список пользователей

Показывает список всех пользователей с пагинацией (20 на страницу).

Для каждого пользователя:
- Имя и username
- License key
- Количество запросов (всего и сегодня)
- Дата последнего запроса

**Примеры:**
```
/users          # Первая страница
/users 2        # Вторая страница
/users 5        # Пятая страница
```

**Кнопки:**
- ◀️ Назад - предыдущая страница
- Вперед ▶️ - следующая страница

---

### 👤 /user <license_key> - Информация о пользователе

Детальная информация о конкретном пользователе:
- Имя, username, Telegram ID
- License key
- Тариф (Free/Premium)
- Статус (активен/заблокирован)
- Лимит запросов
- Дата окончания Premium (если есть)
- Всего запросов и использовано сегодня
- Дата регистрации
- Дата последнего запроса

**Кнопки управления:**
- ⭐ Дать Premium
- ❌ Забрать Premium
- 🚫 Заблокировать / ✅ Разблокировать

**Пример:**
```
/user D5CA-6B7D-4008-8191
```

---

### ⭐ /grant <license_key> [days] - Выдать Premium

Выдаёт Premium подписку пользователю на указанное количество дней.

**Параметры:**
- `license_key` - лицензионный ключ пользователя (обязательно)
- `days` - количество дней (по умолчанию: 365)

**Примеры:**
```
/grant D5CA-6B7D-4008-8191          # Premium на 365 дней
/grant D5CA-6B7D-4008-8191 30       # Premium на 30 дней
/grant D5CA-6B7D-4008-8191 7        # Premium на 7 дней (пробный период)
```

**Что делает:**
- Устанавливает `subscription_tier = "premium"`
- Устанавливает `queries_limit = -1` (безлимит)
- Обнуляет `queries_used_today`
- Устанавливает `premium_until` на текущую дату + указанное количество дней

---

### 📊 /export - Экспорт данных

Экспортирует всех пользователей в CSV файл.

**Столбцы в файле:**
- License Key
- Telegram ID
- Username
- First Name
- Tier (free/premium)
- Queries Total
- Queries Today
- Queries Limit
- Created At
- Last Query At
- Premium Until

**Пример:**
```
/export
```

Бот отправит файл `sheetgpt_users_YYYYMMDD_HHMMSS.csv`

---

## Использование

### 1. Отслеживание роста

Каждый день проверяй `/dashboard` чтобы видеть:
- Сколько новых пользователей за 24 часа
- Сколько пользователей активны
- Какая конверсия в Premium

### 2. Управление пользователями

Если пользователь хочет Premium:
1. Получи от него license key (он может посмотреть в боте через "🔑 Лицензионный ключ")
2. Выполни `/grant <license_key> 365`
3. Подтверди что Premium выдан

### 3. Анализ активности

Используй `/dashboard` чтобы:
- Увидеть топ-5 самых активных пользователей
- Проверить сколько запросов делают пользователи
- Оценить какой средний engagement

### 4. Экспорт для аналитики

Периодически делай `/export` чтобы:
- Сохранить backup данных
- Проанализировать в Excel/Google Sheets
- Построить графики роста
- Рассчитать метрики (retention, churn и т.д.)

---

## Примеры сценариев

### Сценарий 1: Новый платящий пользователь

```
Пользователь: Хочу Premium!
Админ: Отправь мне свой license key из бота (раздел "🔑 Лицензионный ключ")
Пользователь: D5CA-6B7D-4008-8191
Админ: /grant D5CA-6B7D-4008-8191 365
Бот: ✅ Premium выдан!
Админ: Готово! Premium активирован на год
```

### Сценарий 2: Проверка пользователя

```
Админ: /user D5CA-6B7D-4008-8191
Бот: [Показывает детальную информацию]
Админ: [Проверяет активность, запросы, дату регистрации]
```

### Сценарий 3: Еженедельный анализ

```
Админ: /dashboard
Бот: [Показывает статистику]
Админ: [Проверяет]
- Новых пользователей за неделю: 15
- Конверсия в Premium: 8.5%
- Активных за неделю: 42
- Средне запросов на юзера: 12.3

Админ: /export
Бот: [Отправляет CSV]
Админ: [Анализирует в Google Sheets, строит графики]
```

---

## Интерактивные кнопки

### На главном дашборде:

**🔄 Обновить** - обновляет статистику (callback: `admin_refresh`)
**👥 Пользователи** - показывает список пользователей (callback: `admin_users`)
**💎 Premium** - фильтр только Premium пользователи (callback: `admin_premium`)
**🆓 Free** - фильтр только Free пользователи (callback: `admin_free`)
**📊 Экспорт данных** - запускает экспорт в CSV (callback: `admin_export`)

### На карточке пользователя:

**⭐ Дать Premium** - выдает Premium на 365 дней (callback: `admin_grant_{user_id}`)
**❌ Забрать Premium** - забирает Premium (callback: `admin_revoke_{user_id}`)
**🚫 Заблокировать** - блокирует пользователя (callback: `admin_block_{user_id}`)
**✅ Разблокировать** - разблокирует (callback: `admin_unblock_{user_id}`)

---

## Метрики для отслеживания

### Ключевые метрики:

1. **DAU (Daily Active Users)** - активных за 24 часа
2. **WAU (Weekly Active Users)** - активных за 7 дней
3. **Конверсия** - % платящих пользователей
4. **ARPU** - средний доход на пользователя
5. **Retention** - сколько пользователей возвращаются
6. **Churn** - сколько пользователей уходят

### Как считать из дашборда:

**DAU** = "Активных за 24ч" из `/dashboard`
**WAU** = "Активных за 7д" из `/dashboard`
**Конверсия** = (Premium / Всего) * 100% - показывается автоматически
**Avg queries/user** = "Среднее/юзер" из `/dashboard`

---

## Безопасность

⚠️ **ВАЖНО:**

- Команды доступны ТОЛЬКО администратору (ID: `{ADMIN_TELEGRAM_ID}`)
- Если другой пользователь попробует - получит "⛔ Только для администратора"
- License keys содержат конфиденциальную информацию - не публикуй их
- CSV экспорт содержит персональные данные - храни безопасно

---

## Интеграция с существующей системой

Админ-команды уже интегрированы с:
- **Database**: PostgreSQL через SQLAlchemy (async)
- **Models**: `TelegramUser` модель
- **Main bot**: `telegram_bot.py`

Чтобы активировать:
1. Админ ID уже установлен: `ADMIN_TELEGRAM_ID = 517682186`
2. Команды зарегистрированы в `telegram_bot.py` (строки 1012-1017)
3. Используй новые функции из `admin_commands.py`

---

## Следующие шаги

Для подключения новых команд добавь в `telegram_bot.py`:

```python
from app.admin_commands import AdminCommands

# В методе run():
admin_cmds = AdminCommands(self.admin_id, self.async_session_factory)

# Регистрация команд:
self.application.add_handler(CommandHandler("dashboard", admin_cmds.admin_dashboard))
self.application.add_handler(CommandHandler("users", admin_cmds.admin_users_list))
self.application.add_handler(CommandHandler("user", admin_cmds.admin_user_info))
self.application.add_handler(CommandHandler("grant", admin_cmds.admin_grant_premium))
self.application.add_handler(CommandHandler("export", admin_cmds.admin_export_data))
```

---

## FAQ

**Q: Как посмотреть сколько пользователей платят?**
A: `/dashboard` → смотри "Premium: X (Y%)"

**Q: Как найти конкретного пользователя?**
A: Попроси у него license key → `/user <license_key>`

**Q: Как выдать Premium?**
A: `/grant <license_key> 365`

**Q: Можно ли забрать Premium?**
A: Да, через кнопку в `/user <license_key>` или измени напрямую в БД

**Q: Где хранятся данные?**
A: В PostgreSQL, таблица `telegram_users`

**Q: Как часто обновлять статистику?**
A: Рекомендуем проверять `/dashboard` раз в день

---

## Поддержка

Если нужна помощь или новые функции:
- GitHub: https://github.com/Kirill-found/SheetGPT/issues
- Telegram: @Kirill_found
