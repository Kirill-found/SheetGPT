# SheetGPT Frontend - Инструкция по установке и тестированию

## 📋 Что мы создали

Google Apps Script Add-on с красивым Material Design интерфейсом, который:
- Работает прямо в Google Sheets (sidebar справа)
- Генерирует формулы через AI
- Автоматически вставляет результаты в таблицу
- Имеет зеленый дизайн в стиле Google Sheets

---

## 🚀 Установка (пошаговая)

### Шаг 1: Убедись что backend запущен

```bash
cd C:\SheetGPT\backend
python -m uvicorn app.main_simple:app --reload
```

Проверь: http://localhost:8000/health должен вернуть `{"status":"healthy"}`

---

### Шаг 2: Создай новый Apps Script проект

1. Открой любую таблицу в Google Sheets: https://sheets.google.com
2. Создай новую таблицу (или открой существующую)
3. Меню: **Extensions** → **Apps Script**
4. Откроется редактор скриптов

---

### Шаг 3: Скопируй файлы

#### 3.1 Code.gs

В редакторе:
- Удали весь код в `Code.gs`
- Открой файл `C:\SheetGPT\frontend\Code.gs`
- Скопируй **весь** код
- Вставь в редактор Apps Script

#### 3.2 Sidebar.html

В редакторе Apps Script:
- Нажми **+** рядом с "Files"
- Выбери **HTML**
- Назови файл `Sidebar`
- Открой `C:\SheetGPT\frontend\Sidebar.html`
- Скопируй **весь** HTML код
- Вставь в Sidebar.html

#### 3.3 appsscript.json

В редакторе Apps Script:
- Слева нажми на иконку шестерёнки (Project Settings)
- Включи "Show appsscript.json manifest file"
- Теперь слева появится файл `appsscript.json`
- Открой его
- Открой `C:\SheetGPT\frontend\appsscript.json`
- Скопируй **весь** JSON
- Вставь в appsscript.json (заменив всё)

---

### Шаг 4: Настрой API URL (ВАЖНО!)

В файле `Code.gs` в самом начале есть строка:

```javascript
const API_URL = 'http://localhost:8000';
```

**Для локального тестирования:** Оставь как есть

**Для продакшна:** Замени на URL твоего backend на Render.com:
```javascript
const API_URL = 'https://твой-проект.onrender.com';
```

---

### Шаг 5: Сохрани и запусти

1. Нажми **Save** (Ctrl+S)
2. Дай название проекту: "SheetGPT"
3. Закрой редактор Apps Script
4. Обнови страницу таблицы (F5)
5. Появится новое меню **SheetGPT** в верхней панели!

---

## 🧪 Тестирование

### Тест 1: Открыть sidebar

1. В Google Sheets: меню **SheetGPT** → **Открыть AI помощник**
2. Справа должен открыться зеленый sidebar с UI
3. Если не открывается - смотри раздел "Troubleshooting"

---

### Тест 2: Создать тестовые данные

В таблице создай такие данные:

| Дата       | Продажи | Менеджер |
|------------|---------|----------|
| 2024-01-01 | 600000  | Иванов   |
| 2024-01-02 | 400000  | Петров   |
| 2024-01-03 | 700000  | Сидоров  |
| 2024-01-04 | 300000  | Иванов   |

---

### Тест 3: Сгенерировать формулу

1. В sidebar введи запрос:
   ```
   Сумма продаж где сумма больше 500000
   ```

2. Нажми **"Сгенерировать формулу"**

3. Должно произойти:
   - Показать loading (спиннер)
   - Через 2-3 секунды появится результат:
     - Формула: `=SUMIF(B:B, '>500000', B:B)`
     - Объяснение на русском
     - "Формула вставлена в ячейку D1"

4. В таблице в ячейке D1 должна появиться формула!

5. Результат формулы: **1300000** (600000 + 700000)

---

### Тест 4: Примеры запросов

Попробуй нажать на примеры в нижней части sidebar:
- "Средний чек по каждому менеджеру"
- "Количество уникальных клиентов"

Они должны автоматически заполнить поле ввода.

---

## 🎨 Как выглядит UI

```
┌────────────────────────────────────┐
│ SheetGPT AI                  [×]  │ ← Зеленый header
│ Ваш умный помощник              │
│ Free: 5 / 20 запросов           │
├────────────────────────────────────┤
│ ЧТО НУЖНО СДЕЛАТЬ?              │
│ ┌──────────────────────────────┐ │
│ │ Сумма продаж где сумма >    │ │ ← Textarea
│ │ 500000                       │ │
│ └──────────────────────────────┘ │
│ [  Сгенерировать формулу  ]     │ ← Зеленая кнопка
├────────────────────────────────────┤
│ ✓ Формула создана!              │
│ ┌──────────────────────────────┐ │
│ │ =SUMIF(B:B,'>500000',B:B)    │ │ ← Результат
│ └──────────────────────────────┘ │
│ Суммирует значения больше...    │
│ Формула вставлена в D1          │
├────────────────────────────────────┤
│ ПРИМЕРЫ ЗАПРОСОВ                │
│ • Сумма продаж где...           │
│ • Средний чек по...             │
│ • Количество уникальных...      │
└────────────────────────────────────┘
```

---

## ⚠️ Troubleshooting

### Проблема 1: Sidebar не открывается

**Решение:**
1. Проверь консоль ошибок: **View** → **Logs**
2. Возможно нужно дать разрешения:
   - При первом запуске появится диалог "Authorization required"
   - Нажми **Review Permissions**
   - Выбери свой Google аккаунт
   - Нажми **Advanced** → **Go to SheetGPT (unsafe)** ← это нормально для dev
   - Нажми **Allow**

---

### Проблема 2: Ошибка "Ошибка связи с сервером"

**Причины:**
1. Backend не запущен - запусти: `uvicorn app.main_simple:app --reload`
2. Неправильный API_URL в Code.gs - проверь URL
3. CORS ошибка - убедись что backend разрешает localhost

**Проверка:**
```bash
# Проверь что backend работает
curl http://localhost:8000/health
```

---

### Проблема 3: Формула не вставляется

**Причины:**
1. Нет прав на редактирование - убедись что таблица не защищена
2. Ошибка в генерации - смотри текст ошибки в sidebar

**Логи:**
- В Apps Script: **View** → **Logs**
- Смотри ошибки в консоли

---

### Проблема 4: "API key not valid" в логах

**Решение:**
1. Проверь `.env` файл backend:
   ```bash
   cd C:\SheetGPT\backend
   cat .env | grep OPENAI_API_KEY
   ```
2. Убедись что ключ правильный
3. Перезапусти backend

---

## 📝 Что дальше

### Для локального тестирования:

1. **Backend должен быть запущен** на localhost:8000
2. Открываешь таблицу в Google Sheets
3. Меню SheetGPT → Открыть AI помощник
4. Вводишь запрос и получаешь формулу!

---

### Для публикации (после тестирования):

1. **Deploy backend на Render.com**:
   ```bash
   # Будет отдельная инструкция
   ```

2. **Обнови API_URL в Code.gs**:
   ```javascript
   const API_URL = 'https://твой-backend.onrender.com';
   ```

3. **Опубликуй Add-on**:
   - Apps Script Editor → **Deploy** → **New deployment**
   - Type: **Add-on**
   - Configuration: Test deployment или Workspace Add-on

4. **Google Workspace Marketplace**:
   - После тестирования можно опубликовать публично
   - Нужна OAuth verification от Google

---

## 🎯 Функционал (текущий)

✅ **Работает:**
- Открытие sidebar в Google Sheets
- Красивый Material Design UI (зеленый, как Sheets)
- Генерация формул через GPT-4o-mini
- Автоматическая вставка формул в таблицу
- Примеры запросов
- Loading states и анимации
- Отображение ошибок

❌ **TODO (следующие шаги):**
- Анализ данных (endpoint /analyze)
- Генерация отчетов (endpoint /report)
- Аутентификация пользователей
- Подписки и лимиты
- История запросов
- Шаринг формул

---

## 🚀 Быстрый старт (все команды)

```bash
# 1. Запусти backend
cd C:\SheetGPT\backend
python -m uvicorn app.main_simple:app --reload

# 2. Открой Google Sheets
# https://sheets.google.com

# 3. Extensions → Apps Script

# 4. Скопируй файлы из C:\SheetGPT\frontend\

# 5. Сохрани и обнови таблицу

# 6. SheetGPT → Открыть AI помощник

# 7. Готово! 🎉
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи backend: терминал где запущен uvicorn
2. Проверь логи Apps Script: View → Logs
3. Проверь что backend отвечает: http://localhost:8000/health
4. Убедись что API_URL правильный в Code.gs

---

**Создано для SheetGPT MVP** 🚀
