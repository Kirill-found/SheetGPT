# Интеграция Админ-панели

## Текущее состояние

Админ-команды созданы в файле `backend/app/admin_commands.py` и готовы к использованию.

## Бот и админ

**Основной бот**: SheetGPT Telegram Bot (токен в `TELEGRAM_BOT_TOKEN`)
**Админ**: Telegram ID `517682186` (Kirill)

## Как подключить новые команды

### Вариант 1: Добавить в telegram_bot.py

В методе `run()` после строки 1009 добавить:

```python
# Расширенные админ-команды
if self.async_session_factory:
    from app.admin_commands import AdminCommands
    admin_cmds = AdminCommands(self.admin_id, self.async_session_factory)

    # Регистрируем новые команды
    self.application.add_handler(CommandHandler("dashboard", admin_cmds.admin_dashboard))
    self.application.add_handler(CommandHandler("export", admin_cmds.admin_export_data))

    # Переопределяем старые команды на новые (лучшие версии)
    # self.application.add_handler(CommandHandler("users", admin_cmds.admin_users_list))
    # self.application.add_handler(CommandHandler("user", admin_cmds.admin_user_info))

    logger.info("✅ Advanced admin commands registered")
```

**Примечание**: `/users` и `/user` уже зарегистрированы (строки 1013-1014), поэтому можно оставить старые или заменить на новые.

### Вариант 2: Использовать напрямую

Админ-команды можно вызывать напрямую через отдельного бота или API endpoint.

## Список команд

### Уже работают (старые):
- `/stats` - базовая статистика
- `/users` - список пользователей (базовый)
- `/user <license_key>` - инфо о пользователе (базовое)
- `/grant <license_key>` - выдать Premium
- `/revoke <license_key>` - забрать Premium

### Новые (нужно подключить):
- `/dashboard` - **полная админ-панель** с детальной статистикой
- `/export` - экспорт всех пользователей в CSV

### Улучшенные версии (опционально):
- `/users` - с пагинацией и кнопками
- `/user` - с кнопками управления

## Быстрый старт

Если хочешь сразу протестировать новые команды:

1. Открой бота в Telegram
2. Отправь `/stats` - увидишь базовую статистику (уже работает)
3. Отправь `/grant <license_key> 365` - выдать Premium (уже работает)

Чтобы активировать `/dashboard`:
1. Добавь код из "Вариант 1" в `telegram_bot.py`
2. Закоммить и задеплоить на Railway
3. Отправь `/dashboard` в бота

## Проверка текущей конфигурации

```bash
# На Railway проверь переменные окружения:
TELEGRAM_BOT_TOKEN=<токен основного бота>
TELEGRAM_ADMIN_ID=517682186
DATABASE_URL=<postgresql://...>
```

Если `DATABASE_URL` настроен, то все админ-команды будут работать с реальной БД.

## Что уже работает прямо сейчас

Без изменений в коде, ты уже можешь:

1. **Посмотреть статистику:**
   ```
   /stats
   ```
   Покажет количество отзывов, среднюю оценку

2. **Посмотреть пользователей:**
   ```
   /users
   ```
   Покажет последних 20 пользователей с их license keys и статистикой

3. **Информация о конкретном пользователе:**
   ```
   /user D5CA-6B7D-4008-8191
   ```

4. **Выдать Premium:**
   ```
   /grant D5CA-6B7D-4008-8191
   ```

5. **Забрать Premium:**
   ```
   /revoke D5CA-6B7D-4008-8191
   ```

Эти команды работают СЕЙЧАС, просто открой бота и попробуй!

## Тестирование

1. Открой Telegram
2. Найди своего бота (тот у которого токен в `TELEGRAM_BOT_TOKEN`)
3. Отправь `/stats`
4. Если видишь статистику - всё работает!
5. Попробуй `/users` чтобы увидеть список пользователей

## Что улучшит новая админ-панель

Новая `/dashboard` команда добавит:
- 📊 Детальную разбивку по Premium/Free
- 📈 Конверсию в процентах
- ⚡ Активных пользователей за 24ч/7 дней
- 🆕 Новых пользователей за 24ч/7 дней
- 🏆 Топ-5 самых активных
- 🔄 Интерактивные кнопки обновления
- 📊 Экспорт в CSV для аналитики

Но даже без этого, текущие команды уже дают базовую админку!
