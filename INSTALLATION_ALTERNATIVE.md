# Альтернативная установка SheetGPT через script.google.com

Если у вас нет доступа к созданию Apps Script из Google Sheets, используйте этот метод.

## Шаг 1: Создайте standalone проект

1. Откройте https://script.google.com в браузере
2. Нажмите **"Новый проект"** (New project) в левом верхнем углу
3. Проект создастся автоматически

## Шаг 2: Настройте проект

1. Переименуйте проект в "SheetGPT" (нажмите на "Untitled project" вверху)

## Шаг 3: Скопируйте Code.gs

1. В редакторе будет файл `Code.gs`
2. Удалите весь код в нём
3. Откройте файл `C:\SheetGPT\frontend\Code.gs`
4. Скопируйте **весь** код
5. Вставьте в редактор на script.google.com

## Шаг 4: Добавьте Sidebar.html

1. Нажмите **+** рядом с "Файлы"
2. Выберите **HTML**
3. Назовите файл `Sidebar` (без расширения)
4. Откройте `C:\SheetGPT\frontend\Sidebar.html`
5. Скопируйте **весь** код
6. Вставьте в Sidebar.html

## Шаг 5: Настройте appsscript.json

1. Нажмите на иконку шестерёнки (⚙️) слева
2. Включите **"Show 'appsscript.json' manifest file in editor"**
3. Вернитесь к файлам - появится `appsscript.json`
4. Откройте его
5. Откройте `C:\SheetGPT\frontend\appsscript.json`
6. Скопируйте **весь** JSON
7. Вставьте в appsscript.json (замените всё)

## Шаг 6: Обновите API_URL

В файле `Code.gs` в самом начале:
```javascript
const API_URL = 'http://localhost:8000';
```

Оставьте как есть для локального тестирования.

## Шаг 7: Сохраните проект

1. Нажмите **Ctrl+S** или иконку дискеты
2. Дайте разрешения если попросит

## Шаг 8: Протестируйте

У вас есть **ДВА способа** запустить SheetGPT. Рекомендую начать с **Вариант B** (он проще).

---

### ⭐ ВАРИАНТ B: Запустите функцию вручную (ПРОЩЕ - начните с этого!)

Этот способ сразу откроет sidebar и вы увидите результат.

#### 8.1 Найдите выпадающий список функций

В редакторе Apps Script (на странице script.google.com) вверху страницы есть:
- Слева: кнопка "Выполнить" (▶️ Run)
- Справа от неё: **выпадающий список** с названием функции

Сейчас там может быть написано `myFunction` или пусто.

#### 8.2 Выберите функцию showSidebar

1. **Нажмите на этот выпадающий список** (где написано название функции)
2. Появится меню со всеми функциями из вашего кода
3. **Найдите и выберите `showSidebar`**

Список функций:
```
onOpen
onInstall
showSidebar          ← ВЫБИРАЕМ ЭТУ
showWelcome
showHelp
getSheetData
insertFormula
columnToLetter
generateFormula
processQuery
getUserStatus
```

#### 8.3 Нажмите кнопку "Выполнить" (Run)

1. После выбора `showSidebar` нажмите кнопку **▶️ "Выполнить"** (Run) слева от списка
2. Если это первый запуск, появится диалог **"Authorization required"** (требуется авторизация)

#### 8.4 Дайте разрешения (первый запуск)

**ШАГ 1: Review Permissions**
- Появится окно: "Authorization required"
- Нажмите **"Review Permissions"** (Просмотреть разрешения)

**ШАГ 2: Выберите аккаунт**
- Выберите ваш Google аккаунт

**ШАГ 3: Google предупредит что приложение не проверено**
- Появится окно: "This app isn't verified"
- Нажмите **"Advanced"** (Дополнительно) внизу
- Появится ссылка **"Go to SheetGPT (unsafe)"**
- Нажмите на эту ссылку

**ШАГ 4: Разрешите доступ**
- Появится список разрешений:
  - See, edit, create, and delete your spreadsheets in Google Drive
  - Connect to an external service
- Нажмите **"Allow"** (Разрешить)

#### 8.5 Результат

После этого:
1. **Автоматически откроется новая вкладка с Google Sheets**
2. **Справа появится зелёный sidebar SheetGPT!**
3. Это значит что всё работает! 🎉

Если sidebar открылся - **поздравляю, установка завершена!**

---

### 🔗 ВАРИАНТ A: Привяжите к конкретной таблице (для постоянного использования)

Этот способ нужен чтобы меню SheetGPT появилось в **конкретной таблице** при её открытии.

#### 8.6 Найдите панель слева

В редакторе Apps Script (script.google.com) слева есть **боковая панель** с иконками:
```
📁 Файлы (Files)
⚙️  Настройки (Project Settings)
📦 Библиотеки (Libraries)
⏱️  Триггеры (Triggers)
```

#### 8.7 Откройте настройки проекта

1. Нажмите на иконку **⚙️ (шестерёнка)** - это "Project Settings"
2. Откроется страница настроек

#### 8.8 Найдите раздел "Google Cloud Platform (GCP) Project"

Прокрутите вниз до раздела **"Google Cloud Platform (GCP) Project"**

Там есть кнопка **"Change project"**

НО! Нам это не нужно для простого теста.

#### 8.9 АЛЬТЕРНАТИВА: Привяжите через меню "Container"

**ВНИМАНИЕ:** В новом интерфейсе Apps Script эта опция может называться по-другому!

1. Вернитесь к файлам (нажмите 📁 слева)
2. Откройте любую существующую таблицу в Google Sheets: https://sheets.google.com
3. В таблице: меню **"Расширения"** → **"Apps Script"**
4. Появится окно выбора проекта
5. Выберите **"SheetGPT"** (ваш созданный проект)
6. Теперь этот проект привязан к таблице!
7. Обновите таблицу (F5)
8. В верхнем меню должно появиться **SheetGPT**!

---

### ✅ Какой способ выбрать?

**Для первого теста:** Используйте **ВАРИАНТ B** (запуск функции)
- Быстрее
- Сразу видите результат
- Не нужно ничего настраивать

**Для постоянного использования:** Используйте **ВАРИАНТ A** (привязка к таблице)
- Меню SheetGPT будет появляться автоматически
- Удобнее для работы

## Шаг 9: Дайте разрешения

При первом запуске появится диалог:
1. **"Authorization required"**
2. Нажмите **"Review Permissions"**
3. Выберите ваш Google аккаунт
4. Нажмите **"Advanced"** → **"Go to SheetGPT (unsafe)"**
5. Нажмите **"Allow"**

## Проверка работы

1. Откройте таблицу, к которой привязали проект
2. В верхнем меню должно появиться **SheetGPT**
3. Нажмите **SheetGPT** → **"Открыть AI помощник"**
4. Справа откроется зелёный sidebar!

## Если не появилось меню SheetGPT

Убедитесь что:
1. Проект привязан к таблице (Container → Google Sheets)
2. Функция `onOpen()` есть в Code.gs
3. Проект сохранён (Ctrl+S)
4. Таблица обновлена (F5)

## Следующий шаг

После успешной установки:
1. Убедитесь что backend запущен: `python -m uvicorn app.main_simple:app --reload`
2. Проверьте: http://localhost:8000/health
3. В sidebar введите запрос и протестируйте генерацию формулы!

---

**Если возникли проблемы, смотрите:** [FRONTEND_SETUP.md](FRONTEND_SETUP.md) раздел "Troubleshooting"
