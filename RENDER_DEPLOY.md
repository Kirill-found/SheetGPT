# Деплой SheetGPT на Render.com

## 📋 Что мы задеплоим

Backend API SheetGPT на бесплатный хостинг Render.com.

**Бесплатный план:**
- ✅ 750 часов в месяц (достаточно для тестирования)
- ✅ HTTPS из коробки
- ✅ Автоматический деплой из GitHub
- ⚠️ Засыпает после 15 минут неактивности (первый запрос после сна ~30 сек)

---

## 🚀 Пошаговая инструкция

### Шаг 1: Создайте GitHub репозиторий

1. Перейдите на https://github.com
2. Нажмите **"New repository"** (зелёная кнопка)
3. Название: `sheetgpt-backend`
4. Описание: `SheetGPT Backend API`
5. Выберите **Public** или **Private** (без разницы)
6. **НЕ** добавляйте README, .gitignore, license (у нас уже есть!)
7. Нажмите **"Create repository"**

### Шаг 2: Загрузите код на GitHub

Откройте терминал в папке `C:\SheetGPT\backend` и выполните:

```bash
# Инициализируем git репозиторий
git init

# Добавляем все файлы
git add .

# Создаём первый коммит
git commit -m "Initial commit: SheetGPT Backend API"

# Привязываем к GitHub (замените USERNAME на ваш!)
git remote add origin https://github.com/USERNAME/sheetgpt-backend.git

# Отправляем код на GitHub
git branch -M main
git push -u origin main
```

**ВАЖНО:** Замените `USERNAME` на ваш GitHub username!

### Шаг 3: Зарегистрируйтесь на Render.com

1. Перейдите на https://render.com
2. Нажмите **"Get Started"** или **"Sign Up"**
3. Зарегистрируйтесь через **GitHub** (проще всего!)
4. Разрешите Render доступ к вашим репозиториям

### Шаг 4: Создайте Web Service

1. В dashboard Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `sheetgpt-backend`
3. Настройки:
   - **Name:** `sheetgpt-api`
   - **Region:** `Frankfurt` (ближе к России)
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements-minimal.txt`
   - **Start Command:** `uvicorn app.main_simple:app --host 0.0.0.0 --port $PORT`
   - **Plan:** `Free`

4. Нажмите **"Advanced"** и добавьте переменные окружения:

### Шаг 5: Добавьте Environment Variables

Нажмите **"Add Environment Variable"** для каждой:

| Key | Value |
|-----|-------|
| `ENVIRONMENT` | `production` |
| `OPENAI_API_KEY` | `your-openai-api-key-here` |
| `GEMINI_API_KEY` | `your-gemini-api-key-here` |
| `JWT_SECRET_KEY` | `super-secret-jwt-key-for-production-min-32-characters` |
| `CORS_ORIGINS` | `["https://script.google.com", "https://docs.google.com"]` |

**ВАЖНО:** Скопируйте значения ТОЧНО как указано выше!

### Шаг 6: Запустите деплой

1. Нажмите **"Create Web Service"**
2. Render начнёт деплой (займёт 3-5 минут)
3. Следите за логами на странице сервиса

### Шаг 7: Проверьте деплой

После успешного деплоя:

1. Скопируйте URL сервиса (будет вида `https://sheetgpt-api.onrender.com`)
2. Откройте в браузере: `https://sheetgpt-api.onrender.com/health`
3. Должен вернуться: `{"status":"healthy",...}`

✅ **Если видите JSON с "healthy" - всё работает!**

---

## 🔧 Обновление API_URL в Apps Script

После успешного деплоя нужно обновить URL в frontend:

1. Откройте Apps Script редактор
2. Откройте файл **Code.gs**
3. Найдите строку 7:
   ```javascript
   const API_URL = 'https://early-trees-join.loca.lt';
   ```

4. Замените на ваш Render URL:
   ```javascript
   const API_URL = 'https://sheetgpt-api.onrender.com';
   ```

5. Сохраните (**Ctrl+S**)

6. Вернитесь в Google Sheets, обновите страницу (**F5**)

7. Откройте sidebar и протестируйте!

---

## 🐛 Troubleshooting

### Деплой упал с ошибкой

**Проверьте логи:**
- На странице сервиса в Render откройте вкладку **"Logs"**
- Найдите ошибку (обычно в конце)

**Частые проблемы:**
1. **"Module not found"** - проверьте requirements-minimal.txt
2. **"Port already in use"** - это нормально, Render сам управляет портом
3. **"Build failed"** - возможно не хватает памяти (попробуйте requirements-minimal.txt)

### Сервис задеплоился, но /health не отвечает

1. Проверьте что сервис **"Live"** (зелёный статус)
2. Подождите 30-60 секунд (холодный старт)
3. Проверьте логи в Render dashboard

### Apps Script всё ещё ошибка

1. Убедитесь что обновили `API_URL` в Code.gs
2. Убедитесь что сохранили (Ctrl+S)
3. Обновите страницу Google Sheets (F5)
4. Откройте sidebar заново

---

## 📝 После деплоя

### Автоматические обновления

Теперь каждый раз когда вы пушите код на GitHub:
```bash
git add .
git commit -m "Update feature X"
git push
```

Render автоматически задеплоит новую версию!

### Мониторинг

В Render dashboard вы можете:
- Смотреть логи в реальном времени
- Видеть метрики (CPU, память, запросы)
- Перезапускать сервис если нужно

### Бюджет

Бесплатный план:
- ✅ 750 часов/месяц (31 день × 24 часа = 744 часа)
- ✅ Достаточно для одного сервиса 24/7

Если нужно больше - апгрейд до $7/месяц (без сна).

---

## 🎉 Готово!

Теперь у вас есть:
- ✅ Backend на Render.com с постоянным URL
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Полностью рабочий SheetGPT!

**Следующие шаги:**
1. Протестируйте генерацию формул
2. Попробуйте разные запросы
3. Добавьте новые фичи и пушьте на GitHub!

---

**Если что-то пошло не так** - пишите, помогу разобраться! 🚀
